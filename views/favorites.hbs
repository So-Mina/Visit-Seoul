<h1>My Favorites</h1>

{{#each allFavorites}}

<div class="postContainer" data-post-id="{{ _id }}">
  <h2>{{ post.name }}</h2>
  <img id="postImg" src={{post.photo}} alt="">
  <p>{{ post.smallDescription }}</p>
  <a id="postLink" href="/places/{{_id}}">See more... </a>
</div>

{{/each}}


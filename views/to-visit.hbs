<h1>My To Visit List</h1>

<div class="placesContainer">
  
  {{#each allVisits}}

  <div class="postContainer" data-post-id="{{ post._id }}">
    <h2>{{ post.name }}</h2>
    <img id="postImg" src={{post.photo}} alt="">
    <p>{{ post.smallDescription }}</p>
    <a id="postLink" href="/places/{{post._id}}">See more... </a>
    </div>

  {{/each}}

</div>